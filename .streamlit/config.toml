[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
# Isolated port for this repo (trading-system's UI uses the default 8501).
port = 8502
address = "localhost"
headless = true

[browser]
gatherUsageStats = false

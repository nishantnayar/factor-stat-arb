# Run DB tests with a separate test DB: TRADING_DB_NAME=trading_system_test
# See tests/README.md for why and how.
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    database: Database related tests
    trading: Trading system specific tests
    asyncio: Async tests
    logging: Logging tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
